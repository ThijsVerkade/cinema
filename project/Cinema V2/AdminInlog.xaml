<Window x:Name="AdminLogin" x:Class="Cinema_V2.AdminInlog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cinema_V2"
        mc:Ignorable="d"
        Title="AdminInlog" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="DataGridAdmin" HorizontalAlignment="Left" Height="386" Margin="26,10,0,0" VerticalAlignment="Top" Width="740">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Titel" Binding="{Binding Path= Name}"/>
                <DataGridTextColumn Header="Genre" Binding="{Binding Path= Genre}"/>
                <DataGridTextColumn Header="Datum" Binding="{Binding Path= MovieDate}"/>
                <DataGridTextColumn Header="Beschrijving" Binding="{Binding Path= Description}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
